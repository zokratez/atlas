# Atlas State Audit — 2026-07-03

Scope: read-only inventory of Atlas Gen 2 and OpenClaw Gen 1 before revival.
Write performed: this report only.
No services intentionally started. No crontab restored. No `pm2 resurrect`. No paid-provider API calls. No deletions.

## ALIVE

### Gen 1 OpenClaw PM2 processes are online

`pm2 ls` reports four online processes, all up about 20h:

```text
elcrypto-engine    online pid 1681
kalshi-poller      online pid 1678
risk-manager       online pid 1679
trader-executor    online pid 1680
```

This is the most important audit result. The expected state was "all stopped"; actual state is active trading/elCrypto infrastructure.

### PM2 daemon state exists

`~/.pm2/` contains active PM2 runtime artifacts:

```text
dump.pm2
dump.pm2.bak
logs/
pids/
pm2.log
pm2.pid
pub.sock
rpc.sock
```

`dump.pm2` exists, which means `pm2 resurrect` has something to restore.

### Auto-start files exist

Two LaunchAgent files exist:

```text
/Users/samoteo/Library/LaunchAgents/ai.openclaw.gateway.plist
/Users/samoteo/Library/LaunchAgents/pm2.samoteo.plist
```

`launchctl list | grep -iE "openclaw|atlas"` returned no loaded matching labels, but files in `~/Library/LaunchAgents/` can still create reboot/login risk.

`ai.openclaw.gateway.plist` contains:

```text
Label: ai.openclaw.gateway
Program: /usr/local/lib/node_modules/openclaw/dist/index.js gateway --port 18789
RunAtLoad: true
KeepAlive: true
Environment key present: ANTHROPIC_API_KEY value present but redacted from this report
```

`pm2.samoteo.plist` contains:

```text
Label: com.PM2
Program: /usr/local/lib/node_modules/pm2/bin/pm2 resurrect
RunAtLoad: true
KeepAlive: true
PM2_HOME: /Users/samoteo/.pm2
```

### Atlas Gen 2 repo is alive as a repo

`~/Code/atlas` exists and is a git repo. Current recent commits:

```text
83ff326 docs(atlas): add registry and durable intake rules to growth skill
90c732a docs(memory): add cross-project memory system
d737489 feat(atlas): durable intake bridge for Hermes and Telegram
c5ae1e9 feat(atlas): add bounded loop operator and intake workflow
746af93 feat(atlas): add growth R&D intake and validation operator
```

`git status --short --ignored` shows:

```text
?? vault/05-intake/2026-06-11-2133-atlas-intake-smoke-test-analyze-this-marketing-source-claim-durable-inta.md
?? vault/05-intake/2026-06-11-2133-atlas-intake-smoke-test.md
!! .claude/
!! hermes-data/
!! vault/.DS_Store
```

The repo is not clean: two intake files are untracked. `hermes-data/` is present locally and ignored.

### Atlas Gen 2 local runtime data exists

`find ~/Code/atlas -maxdepth 3 -type f | head -100` shows local runtime files under `hermes-data/`, including:

```text
hermes-data/.env
hermes-data/state.db
hermes-data/config.yaml
hermes-data/kanban.db
hermes-data/models_dev_cache.json
hermes-data/channel_directory.json
hermes-data/sessions/*.json
hermes-data/logs/*.log
hermes-data/auth.json
hermes-data/cron/jobs.json
```

This is local-only runtime state, not necessarily tracked. Presence of `hermes-data/cron/jobs.json` matters before any Atlas revival.

### Docker state could not be confirmed

`docker ps --format ...` failed because Docker socket was unavailable:

```text
failed to connect to the docker API at unix:///Users/samoteo/.docker/run/docker.sock
```

So Hermes container liveness was not confirmed in this audit.

## SALVAGEABLE

### Atlas Gen 2 repo structure

`ls -la ~/Code/atlas`:

```text
.claude/
.git/
.gitignore
BUILD_REPORT.md
CLAUDE.md
LICENSE
README.md
SETUP_TODO_FOR_SAM.md
docker-compose.yml
docs/
hermes-data/
scripts/
skills/
vault/
```

This is salvageable as the successor system, but not ready to revive blindly because it has local runtime state, untracked intake, and Hermes cron metadata.

### Atlas Gen 2 skills

`~/Code/atlas/skills/` contains three Hermes skills:

```text
skills/atlas-growth-rd/SKILL.md
skills/atlas-loop-operator/SKILL.md
skills/huh-channel-strategy/SKILL.md
```

Skill inventory:

- `atlas-growth-rd`: validates marketing sources, grades evidence, maps findings to products, and turns ideas into bounded experiments.
- `atlas-loop-operator`: defines bounded Atlas loops with max iterations, budget, stop conditions, and human approval gates.
- `huh-channel-strategy`: enforces Huh? first-90-day channel doctrine around TikTok, Shorts/Reels, Reddit, and Diego screen-recording demos.

### `scripts/sync-skills.sh` exists

Contents:

```bash
#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
container="${ATLAS_CONTAINER:-hermes-atlas}"
source_dir="$repo_root/skills"
target_dir="/opt/data/skills"

if ! docker ps --format '{{.Names}}' | grep -qx "$container"; then
  echo "Container '$container' is not running." >&2
  exit 1
fi

if [[ ! -d "$source_dir" ]]; then
  echo "Missing skills directory: $source_dir" >&2
  exit 1
fi

docker exec "$container" mkdir -p "$target_dir"

shopt -s nullglob
for skill_dir in "$source_dir"/*; do
  [[ -d "$skill_dir" ]] || continue
  docker cp "$skill_dir" "$container:$target_dir/"
done

echo "Skills now present in $container:$target_dir"
docker exec "$container" ls -1 "$target_dir"
```

Risk: safe as a manual sync tool, but it writes into the Hermes container if run. Do not run until the target container and skill set are intentionally selected.

### Atlas Gen 2 docs and loop/intake material

The file inventory shows Gen 2 has grown beyond the original build:

```text
docs/LOOP-OPERATING-MANUAL.md
scripts/atlas-intake-bridge.py
scripts/hermes-atlas-intake-hook.sh
scripts/intake-source.sh
scripts/process-intake-drop.sh
vault/05-intake/
vault/06-opportunities/
vault/07-validated/
vault/08-experiments/
vault/09-swipe-file/
vault/10-loop-specs/
```

This looks salvageable as the durable intake / growth R&D layer, but every loop and hook needs review before revival.

### OpenClaw workspace memory/docs

`~/.openclaw/workspace/` contains `USER.md` and `TOOLS.md`, as expected. It also contains many potentially useful docs and logs:

```text
AGENTS.md
BOOTSTRAP.md
HEARTBEAT.md
IDENTITY.md
MEMORY.md
SOUL.md
TOOLS.md
USER.md
learnings.md
loss-log.md
marketing/
mission-control/
products/
scripts/
skills/
trading/
```

OpenClaw memory is salvageable as historical context only, not as a runnable system.

### OpenClaw agent doctrine files

Actual agents found under `~/.openclaw/agents/`:

```text
boost
buzz
flow
lens
marketing
master
production
quill
risk-manager
scout
trader
trading-intel
```

Expected names from the brief map to actual directories like this:

```text
master      -> master / Musk
lola        -> marketing / Lola
cherubim    -> risk-manager / Cherubim
nephi       -> trader / Nephi
radar       -> trading-intel / Radar
production  -> production
```

No literal `lola`, `cherubim`, `nephi`, or `radar` directories were present under `~/.openclaw/agents/`.

Head summaries from `SOUL.md` files:

- `master`: Musk master orchestrator, routes work, Telegram direct conversation mode.
- `marketing`: Lola marketing intelligence agent under Musk.
- `production`: content execution agent that publishes/schedules content from Lola.
- `risk-manager`: Cherubim, validates trades and guards capital.
- `trader`: Nephi, executes Kalshi trades after risk-manager approval.
- `trading-intel`: Radar, monitors Kalshi leaderboard/feed and sends signals.
- `boost`: paid media agent.
- `buzz`: social distribution/community/PR.
- `flow`: email/lifecycle/retention.
- `lens`: analytics/measurement/compliance.
- `quill`: copy/content/creative direction.
- `scout`: research and discoverability intelligence.

### ooabisabi memory folder

`git -C ~/ooabisabi-memory log --oneline -5`:

```text
2ef1753 docs(memory): add tools registry and intake dump card
88e4ca7 docs(memory): add PACO strategy snapshot and project stubs
00ad801 docs(memory): add Atlas loop and Telegram workflow
db4af2 docs(memory): add Atlas Growth R&D operating memory
0cff45f docs(ops): add PACO Health operating registry
```

`~/ooabisabi-memory/` contains:

```text
Archive-Guide.md
CLAUDE.md
Context.md
Instructions.md
Memory.md
how-to-dump.md
tools-registry.md
atlas/
huh/
paco/
paper-wings/
restaurant-90days/
sessions/
wiki/
```

This is highly salvageable as the source of durable operating memory.

## DEAD

### No crontab installed for current user

`crontab -l` result:

```text
crontab: no crontab for samoteo
```

So there is no user crontab to restore or kill.

### No loaded OpenClaw/Atlas launchctl labels found by grep

`launchctl list | grep -iE "openclaw|atlas"` returned no output.

Important distinction: no loaded matching launchctl label was visible, but LaunchAgent files still exist and are reboot/login risk.

### Docker/Hermes runtime not reachable during audit

Docker socket was unavailable. That makes the Docker/Hermes runtime dead or unavailable from this audit context, but not safely classified as removed.

### Old expected OpenClaw agent names are not literal directories

These expected names were not present as direct directories:

```text
radar
cherubim
nephi
lola
```

They survive by role alias in `trading-intel`, `risk-manager`, `trader`, and `marketing`.

## COST-RISK

### Critical: PM2 trading/elCrypto stack is online now

Online processes:

```text
elcrypto-engine
kalshi-poller
risk-manager
trader-executor
```

Risk: could continue polling, trading, invoking APIs, or spending money depending on credentials/config.

### Critical: PM2 LaunchAgent can resurrect processes on login/reboot

`/Users/samoteo/Library/LaunchAgents/pm2.samoteo.plist` runs:

```text
/usr/local/lib/node_modules/pm2/bin/pm2 resurrect
```

With `RunAtLoad: true` and `KeepAlive: true`.

Risk: even if PM2 processes are stopped manually, login/reboot can restore the dump.

### Critical: OpenClaw Gateway LaunchAgent can auto-start with provider key

`/Users/samoteo/Library/LaunchAgents/ai.openclaw.gateway.plist` has:

```text
RunAtLoad: true
KeepAlive: true
Program: openclaw gateway --port 18789
ANTHROPIC_API_KEY present in EnvironmentVariables
```

Risk: OpenClaw gateway can restart and spend Anthropic credits if invoked. The key value exists in the plist and should be treated as exposed/local secret material. The value is intentionally not included in this report.

### Secret-bearing files present in OpenClaw

Presence only, values not read into this report:

```text
/Users/samoteo/.openclaw/agents/marketing/agent/auth-profiles.json
/Users/samoteo/.openclaw/agents/master/agent/auth-profiles.json
/Users/samoteo/.openclaw/agents/risk-manager/agent/auth-profiles.json
/Users/samoteo/.openclaw/agents/trader/agent/auth-profiles.json
/Users/samoteo/.openclaw/agents/trading-intel/agent/auth-profiles.json
/Users/samoteo/.openclaw/kalshi_private_key.pem
/Users/samoteo/.openclaw/workspace/anthropic_key.txt
/Users/samoteo/.openclaw/workspace/telegram_token.txt
```

Additional filename-only grep found key/token/auth references in session files, workspace docs/scripts, OpenClaw logs, and trading config files.

### Atlas Gen 2 local secrets/runtime present

Local Atlas runtime includes:

```text
/Users/samoteo/Code/atlas/hermes-data/.env
/Users/samoteo/Code/atlas/hermes-data/auth.json
/Users/samoteo/Code/atlas/hermes-data/cron/jobs.json
```

Risk: if Hermes is revived as-is, existing cron jobs or auth state may reactivate. Values were not printed.

### OpenClaw trading code present

`~/.openclaw/workspace/trading/` exists and includes trading executor/config code. `trader` and `risk-manager` agents explicitly reference Kalshi API execution.

Risk: high. Treat as money-moving infrastructure until proven disabled.

## RECOMMENDED KILL LIST

Do not execute these actions yet; this is the recommended kill list for the next brief.

1. Stop PM2 apps: `elcrypto-engine`, `kalshi-poller`, `risk-manager`, `trader-executor`.
2. Disable PM2 resurrect LaunchAgent: `/Users/samoteo/Library/LaunchAgents/pm2.samoteo.plist`.
3. Remove or unload OpenClaw gateway autostart: `/Users/samoteo/Library/LaunchAgents/ai.openclaw.gateway.plist`.
4. Rotate the Anthropic key found in the OpenClaw gateway plist.
5. Quarantine OpenClaw trading secrets before any revival:
   - Kalshi private key.
   - OpenClaw `auth-profiles.json` files.
   - Anthropic/Telegram text key files.
6. Treat all OpenClaw trading/elCrypto components as dead unless Sam explicitly revives them:
   - `elcrypto-engine`
   - `kalshi-poller`
   - `trader-executor`
   - `risk-manager`
   - `trader`
   - `trading-intel`
7. Do not run `pm2 resurrect` until the PM2 dump has been reviewed and edited.
8. Do not start Atlas/Hermes until `hermes-data/cron/jobs.json`, `.env`, and `auth.json` are reviewed.
9. Do not run `scripts/sync-skills.sh` until the target Hermes container is intentionally selected.
10. Keep Atlas Gen 2 repo/vault/skills as the revival base; use OpenClaw only as archived doctrine and historical learning.

## RAW INVENTORY APPENDIX

### Gen 2 `ls -la ~/Code/atlas`

```text
total 80
drwxr-xr-x  16 samoteo  staff   512 May 27 22:00 .
drwxr-xr-x   6 samoteo  staff   192 Jun  7 23:19 ..
drwxr-xr-x@  3 samoteo  staff    96 May 26 22:10 .claude
drwxr-xr-x@ 12 samoteo  staff   384 Jun 11 21:56 .git
-rw-r--r--@  1 samoteo  staff   340 May 27 21:42 .gitignore
-rw-r--r--@  1 samoteo  staff  8935 May 27 22:00 BUILD_REPORT.md
-rw-r--r--@  1 samoteo  staff  2369 May 26 21:50 CLAUDE.md
-rw-r--r--@  1 samoteo  staff  1065 May 27 21:57 LICENSE
-rw-r--r--@  1 samoteo  staff  4747 Jun  8 21:25 README.md
-rw-r--r--@  1 samoteo  staff   899 Jun  8 21:25 SETUP_TODO_FOR_SAM.md
-rw-r--r--@  1 samoteo  staff   544 Jun  8 21:23 docker-compose.yml
drwxr-xr-x@  6 samoteo  staff   192 Jun  8 20:11 docs
drwx------@ 36 samoteo  staff  1152 Jun 15 11:53 hermes-data
drwxr-xr-x@  9 samoteo  staff   288 Jun  8 21:27 scripts
drwxr-xr-x@  5 samoteo  staff   160 Jun  8 20:08 skills
drwxr-xr-x@ 14 samoteo  staff   448 Jun  8 20:20 vault
```

### OpenClaw agents directory

```text
boost
buzz
flow
lens
main
marketing
master
production
quill
risk-manager
scout
trader
trading-intel
```

`main/` exists but no `SOUL.md` was found at max depth 2 for it.

### OpenClaw workspace confirms USER/TOOLS

`~/.openclaw/workspace/` includes:

```text
USER.md
TOOLS.md
```

### Schedule state summary

```text
pm2 ls: four online apps, including elcrypto-engine
crontab -l: no crontab for samoteo
launchctl list grep openclaw|atlas: no output
LaunchAgents: ai.openclaw.gateway.plist and pm2.samoteo.plist exist
```

## CONTAINMENT EVIDENCE

Containment evidence captured before stopping on 2026-07-03.

### PM2 snapshot before stopping

`pm2 ls` showed four online OpenClaw processes:

```text
name              status   uptime   restarts   cpu   mem
elcrypto-engine  online   20h      0          0%    136.3mb
kalshi-poller    online   20h      0          0%    46.6mb
risk-manager     online   20h      0          0%    21.7mb
trader-executor  online   20h      0          0%    22.8mb
```

### PM2 describe summary before stopping

`pm2 describe` was run for all four exact process names. Raw output exposed an Anthropic environment variable, so only redacted/safe fields are recorded here.

```text
elcrypto-engine
status: online
uptime: 20h
restarts: 0
script path: /Users/samoteo/.openclaw/workspace/trading/elcrypto-engine.js
exec cwd: /Users/samoteo/.openclaw/workspace/trading
node.js version: 24.13.1
secret env present: ANTHROPIC_API_KEY present, value redacted

kalshi-poller
status: online
uptime: 20h
restarts: 0
script path: /Users/samoteo/.openclaw/workspace/trading/kalshi-profile-poller.js
exec cwd: /Users/samoteo/.openclaw/workspace/trading
node.js version: 24.13.1
node env: production
secret env present: ANTHROPIC_API_KEY present, value redacted

risk-manager
status: online
uptime: 20h
restarts: 0
script path: /Users/samoteo/.openclaw/workspace/trading/risk-manager.js
exec cwd: /Users/samoteo/.openclaw/workspace/trading
node.js version: 24.13.1
secret env present: ANTHROPIC_API_KEY present, value redacted

trader-executor
status: online
uptime: 20h
restarts: 0
script path: /Users/samoteo/.openclaw/workspace/trading/trader-executor.js
exec cwd: /Users/samoteo/.openclaw/workspace/trading
node.js version: 24.13.1
secret env present: ANTHROPIC_API_KEY present, value redacted
```

## CONTAINMENT COMPLETE

Containment actions performed on 2026-07-03 under the requested rules: stopped processes only, no file deletion, no API key edits, no `pm2 resurrect`, no `pm2 start`.

### PM2 stop and save

Command run:

```bash
pm2 stop elcrypto-engine kalshi-poller risk-manager trader-executor
pm2 save --force
```

Stop result:

```text
elcrypto-engine stopped
kalshi-poller stopped
risk-manager stopped
trader-executor stopped
```

Final `pm2 ls` verification:

```text
name              status    pid   uptime   restarts   cpu   mem
elcrypto-engine  stopped   0     0        0          0%    0b
kalshi-poller    stopped   0     0        0          0%    0b
risk-manager     stopped   0     0        0          0%    0b
trader-executor  stopped   0     0        0          0%    0b
```

`pm2 save --force` result:

```text
[PM2] Successfully saved in /Users/samoteo/.pm2/dump.pm2
```

### Autostart disabled and renamed

Matching LaunchAgent files before containment:

```text
ai.openclaw.gateway.plist
pm2.samoteo.plist
```

Labels:

```text
ai.openclaw.gateway
com.PM2
```

Actions:

```bash
launchctl bootout gui/501/ai.openclaw.gateway || true
launchctl disable gui/501/ai.openclaw.gateway
mv /Users/samoteo/Library/LaunchAgents/ai.openclaw.gateway.plist /Users/samoteo/Library/LaunchAgents/ai.openclaw.gateway.plist.DISABLED-2026-07-03

launchctl bootout gui/501/com.PM2 || true
launchctl disable gui/501/com.PM2
mv /Users/samoteo/Library/LaunchAgents/pm2.samoteo.plist /Users/samoteo/Library/LaunchAgents/pm2.samoteo.plist.DISABLED-2026-07-03
```

Notes:

```text
OpenClaw bootout returned: Boot-out failed: 3: No such process
This means the label was not loaded at bootout time. Disable and rename still completed.
```

Renamed files now present:

```text
/Users/samoteo/Library/LaunchAgents/ai.openclaw.gateway.plist.DISABLED-2026-07-03
/Users/samoteo/Library/LaunchAgents/pm2.samoteo.plist.DISABLED-2026-07-03
```

Disabled labels verified:

```text
"ai.openclaw.gateway" => disabled
"com.PM2" => disabled
```

Launchctl list verification:

```text
launchctl list | grep -iE "openclaw|atlas|elcrypto" returned no output
launchctl list | grep -iE "pm2|openclaw|atlas|elcrypto" returned no output
```

### Crontab verification

`crontab -l` result:

```text
crontab: no crontab for samoteo
```

No active crontab lines existed, so nothing was edited.

### Current containment status

```text
OpenClaw PM2 trading/elCrypto processes: stopped
PM2 stopped state: saved
OpenClaw gateway LaunchAgent: disabled and renamed
PM2 resurrect LaunchAgent: disabled and renamed
crontab: absent
files deleted: none
API keys touched: none
pm2 resurrect/start: not run
```
